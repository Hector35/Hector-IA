{
  "version": 4,
  "updated": "2026-08-22",
  "principle": "Use shared context to make informed architectural decisions. Reuse is preferred when it is better, but the registry is context, not an internal permission gate.",
  "coordination": {
    "mode": "advisory",
    "sharedLedger": "https://github.com/Hector35/Hector-IA/issues/958",
    "rule": "Inspect current main, open PRs, Context Hub and the shared ledger before duplicating a capability. Claims and recommendations are evidence, never locks."
  },
  "installablePwas": [
    {
      "id": "hector-os",
      "name": "Héctor OS",
      "status": "active",
      "canonicalPath": "/",
      "manifest": "/manifest.webmanifest",
      "serviceWorker": "/sw.js",
      "purpose": "Main personal OS, chat, general controls, shared utilities and entry point.",
      "owns": ["general-ui", "chat", "shared-tools", "bridge-ui", "context-ui"]
    },
    {
      "id": "hector-agent",
      "name": "Héctor Agent",
      "status": "active",
      "canonicalPath": "/agent/",
      "aliases": ["/agent", "/hector-agent", "/hector-agent/"],
      "manifest": "/agent/manifest.webmanifest",
      "serviceWorker": "/agent/sw.js",
      "purpose": "Autonomous goals, task execution, approvals, activity, checkpoints and Agent controls.",
      "owns": ["autonomy-ui", "goals", "jobs", "approvals", "agent-memory-controls"]
    },
    {
      "id": "pendientes",
      "name": "Pendientes",
      "status": "active-protected",
      "canonicalPath": "/turno-rx/",
      "manifest": "/turno-rx/manifest.webmanifest",
      "serviceWorker": "/turno-rx/sw.js",
      "purpose": "Clinical Turno RX/Pendientes workflow.",
      "owns": ["piso", "rx", "tac", "usg", "clinical-local-data"],
      "protected": true,
      "rule": "Do not modify for unrelated work. Work explicitly targeting Pendientes may change it normally."
    }
  ],
  "sharedSurfaces": [
    {
      "id": "hector-bridge",
      "kind": "service-and-pages",
      "ownerPwa": "hector-os",
      "canonicalUi": "/bridge.html",
      "auxiliaryUi": ["/bridge-core.html"],
      "backend": "/api/hector-bridge",
      "remoteMcp": "/mcp",
      "status": "active",
      "rule": "Bridge, Bridge Core, Context tools and remote MCP are one shared capability layer owned by Héctor OS."
    },
    {
      "id": "context-hub",
      "kind": "backend-service",
      "ownerPwa": "hector-os",
      "backend": "/api/context-hub",
      "status": "active",
      "rule": "Context Hub is shared context infrastructure, not an installable PWA."
    }
  ],
  "creationRules": [
    "Before creating or splitting a surface, inspect shared context and concurrent work so the decision is informed rather than accidental.",
    "Prefer an existing owner when it is the simplest clean architecture; do not duplicate by ignorance.",
    "A new PWA or surface may be created without an internal approval token when evidence shows that separation is technically better.",
    "The same change that creates a new installable PWA must update this registry with a unique path, manifest and service worker.",
    "Never create overlapping service-worker scopes or cache ownership across PWAs.",
    "Do not modify Pendientes for unrelated work.",
    "Claims, registry entries and shared-context records are advisory coordination signals and must not become permission gates."
  ]
}
