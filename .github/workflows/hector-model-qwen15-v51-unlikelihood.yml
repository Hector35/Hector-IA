name: Hector model Qwen15 v51 unlikelihood
on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/hector-model-qwen15-v51-unlikelihood.yml'
      - 'model/hector-asi/training/train_qwen15_v51_unlikelihood.py'
      - 'model/hector-asi/data/qwen15-v51-unlikelihood-train.jsonl'
      - 'model/hector-asi/evals/qwen15-v51-unlikelihood-validation.jsonl'
      - 'model/hector-asi/evals/qwen15-v51-hidden.jsonl'
  workflow_dispatch:
permissions:
  contents: read
  actions: read
concurrency:
  group: hector-qwen15-v51-${{ github.ref }}
  cancel-in-progress: false
jobs:
  train-qwen15-v51:
    runs-on: ubuntu-latest
    timeout-minutes: 150
    env:
      SOURCE_ARTIFACT_ID: '8560331055'
      SOURCE_SHA: '31bead5f59982d8e321517fd235b81f992f83d2e3792a55690c2a841ff0e28f8'
      SOURCE_BYTES: '73911112'
      SOURCE_DIR: /tmp/hector-v41/adapter
      TRAIN: model/hector-asi/data/qwen15-v51-unlikelihood-train.jsonl
      VALIDATION: model/hector-asi/evals/qwen15-v51-unlikelihood-validation.jsonl
      HIDDEN: model/hector-asi/evals/qwen15-v51-hidden.jsonl
      OUT_A: artifacts/hector-asi-qwen15-v51-a
      OUT_B: artifacts/hector-asi-qwen15-v51-b
      TOKENIZERS_PARALLELISM: 'false'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Validate licensed data, splits and trainer
        run: |
          python -m py_compile model/hector-asi/training/train_qwen15_v51_unlikelihood.py
          python - <<'PY'
          import json,unicodedata
          from pathlib import Path
          def norm(s): return ' '.join(unicodedata.normalize('NFKD',s).encode('ascii','ignore').decode().lower().split())
          tr=[json.loads(x) for x in Path('${{ env.TRAIN }}').read_text().splitlines() if x.strip()]
          va=[json.loads(x) for x in Path('${{ env.VALIDATION }}').read_text().splitlines() if x.strip()]
          hi=[json.loads(x) for x in Path('${{ env.HIDDEN }}').read_text().splitlines() if x.strip()]
          assert (len(tr),len(va),len(hi))==(16,6,18)
          assert all(x.get('verified') is True and x.get('provenance')=='author-generated' and x.get('license')=='Apache-2.0-compatible' for x in tr+va)
          groups=[{norm(x['prompt']) for x in rows} for rows in (tr,va,hi)]
          assert not groups[0]&groups[1] and not groups[0]&groups[2] and not groups[1]&groups[2]
          assert sum(bool(x.get('critical')) for x in hi)==6
          print('train=16 validation=6 hidden=18 critical=6 contamination=0')
          PY
      - name: Download and verify immutable champion v41
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir -p /tmp/v41zip /tmp/hector-v41
          curl --fail --location --retry 4 -H "Authorization: Bearer $GH_TOKEN" -H "Accept: application/vnd.github+json" "https://api.github.com/repos/${GITHUB_REPOSITORY}/actions/artifacts/${SOURCE_ARTIFACT_ID}/zip" -o /tmp/v41.zip
          unzip -q /tmp/v41.zip -d /tmp/v41zip
          F="$(find /tmp/v41zip -type f -path '*/adapter/adapter_model.safetensors' -print -quit)"
          test -n "$F"; test "$(sha256sum "$F" | awk '{print $1}')" = "$SOURCE_SHA"; test "$(stat -c%s "$F")" -eq "$SOURCE_BYTES"
          cp -a "$(dirname "$F")" "$SOURCE_DIR"
      - name: Install pinned CPU stack
        run: |
          python -m pip install --disable-pip-version-check --index-url https://download.pytorch.org/whl/cpu torch==2.7.1
          python -m pip install --disable-pip-version-check accelerate==1.8.1 huggingface_hub==0.34.4 peft==0.16.0 safetensors==0.5.3 transformers==4.53.2
      - name: Train two deterministic real replicas
        run: |
          set -euo pipefail
          /usr/bin/time -v python model/hector-asi/training/train_qwen15_v51_unlikelihood.py --source-adapter "$SOURCE_DIR" --train "$TRAIN" --validation "$VALIDATION" --hidden "$HIDDEN" --out "$OUT_A" --seed 3051 2>&1 | tee /tmp/v51-a.log
          /usr/bin/time -v python model/hector-asi/training/train_qwen15_v51_unlikelihood.py --source-adapter "$SOURCE_DIR" --train "$TRAIN" --validation "$VALIDATION" --hidden "$HIDDEN" --out "$OUT_B" --seed 3051 2>&1 | tee /tmp/v51-b.log
      - name: Verify reproducibility and promotion evidence
        run: |
          set -euo pipefail
          A="$OUT_A/adapter/adapter_model.safetensors"; B="$OUT_B/adapter/adapter_model.safetensors"
          test -f "$A" -a -f "$B"; cmp "$A" "$B"; test "$(stat -c%s "$A")" -gt 50000000
          python - <<'PY'
          import json
          from pathlib import Path
          a=json.loads(Path('artifacts/hector-asi-qwen15-v51-a/metrics.json').read_text())
          b=json.loads(Path('artifacts/hector-asi-qwen15-v51-b/metrics.json').read_text())
          assert a['candidate']['adapterSha256']==b['candidate']['adapterSha256']
          assert a['candidate']['adapterBytes']==b['candidate']['adapterBytes']
          assert a['candidate']['parameterL2']>0 and a['gates']['validationObjectiveImproved']
          print(json.dumps({'sha256':a['candidate']['adapterSha256'],'bytes':a['candidate']['adapterBytes'],'baseline':a['baseline']['behavioral'],'candidate':a['candidate']['behavioral'],'gates':a['gates']},indent=2,sort_keys=True))
          PY
      - name: Upload weights, metrics and logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: hector-asi-qwen15-v51-unlikelihood-${{ github.run_id }}
          path: |
            artifacts/hector-asi-qwen15-v51-a
            artifacts/hector-asi-qwen15-v51-b
            /tmp/v51-a.log
            /tmp/v51-b.log
          retention-days: 90
          if-no-files-found: warn
