import { syncRowsFromStorageAndRender, canonicalOrigin, normalizeAge, normalizeStudyDisplay } from './app-v16.js?v=65';

const STORAGE_KEY='pendientes-table-v2';
const SHIFT_KEY='pendientes-shift-v1';
const ACTIVE_TAB_KEY='pendientes-active-category-v49';
const BUILD='75';
const DB_NAME='pendientes-boleta-images-v1';
const STORE='images';

const VISION_PROMPT=`Analiza esta foto de una boleta, solicitud o pizarrón hospitalario para la PWA Pendientes. Devuelve SOLO JSON válido, sin markdown:
{"patients":[{"category":"Rayos X|TAC|USG|Piso|Interconsulta|Apoyo para movimiento","handwrittenBed":"","formBed":"","bed":"","name":"","birthDate":null,"age":null,"sex":"Mujer|Hombre|No visible","target":"","destination":"","destinationFloor":"","destinationBlock":"","destinationService":"","modality":"Rayos X|TAC|Ultrasonido|Otro","region":"","withContrast":false,"diagnosis":"","diagnosisMeaning":"","requestingDoctor":"","folio":"","service":"","originService":"","requestDate":"","requestTime":"","transferNotes":"","extraData":{},"recognizedText":"","confidence":{"bed":"high|medium|low","name":"high|medium|low","age":"high|medium|low","sex":"high|medium|low","target":"high|medium|low"},"transport":"Silla|Camilla|No trasladar|Por definir","transportReason":"","oxygenProbable":false,"oxygenReason":""}]}.

REGLAS GENERALES:
- Extrae únicamente lo visible. No inventes datos clínicos, nombres, camas, destinos, servicios ni hechos.
- Conserva TODO dato legible de la boleta. Los campos que no tengan una clave específica van en extraData con nombres descriptivos.
- recognizedText conserva una transcripción útil de todo lo legible para revisión.
- CE1, CE2, CE3, CE4, UP, UP1, UI1 y similares son áreas, nunca cama 1. "C/ CE4" significa CE4.
- Sala de espera nunca sustituye una cama manuscrita visible.
- Si un campo es dudoso usa confidence low y no lo completes por contexto.

PISO / PIZARRÓN:
- "PISO = 11" o cualquier total de encabezado es SOLO un total. Nunca crea pacientes por sí mismo.
- Lee cada renglón físico por separado. Solo crea un paciente válido cuando exista cama/área real de origen. Si se ve información parcial pero no cama, devuelve un objeto parcial con recognizedText, sin inventar cama.
- En pizarrones con columnas H y M, H=Hombre y M=Mujer según la columna física del renglón.
- bed es la cama/área actual de origen. destination es la cama destino visible si existe.
- Un servicio junto a "sube a", "va a", "ingresa a", "pasa a" o equivalente es destinationService, NO originService.
- originService solo se usa si la procedencia está explícita. No copies automáticamente service a originService.
- Si se lee Nefrología, Cirugía/Trauma, Medicina Interna, Obstetricia, Pediatría o Ginecología como destino, guárdalo en destinationService. La app resolverá el piso.
- No inventes destinationFloor ni destinationBlock si no son visibles; pueden quedar vacíos.

IMAGENOLOGÍA:
- TAC/TC/tomografía/AngioTAC => TAC. USG/ultrasonido/ecografía => USG. RX/radiografía/placa/tele => Rayos X.
- requestingDoctor, folio, requestDate, requestTime, service, originService, transferNotes, diagnóstico y cualquier otro dato visible deben conservarse.
- Si una boleta solicita modalidades distintas, devuelve un objeto por modalidad compartiendo la metadata de la misma boleta.

TRASLADO:
- Es una estimación operativa conservadora, no una orden médica.
- Si dice PORTÁTIL usa No trasladar.
- Usa Camilla solo con evidencia visible fuerte de que debe ir acostado o no puede movilizarse con seguridad (por ejemplo inmovilidad explícita, trauma/fractura importante, déficit neurológico marcado, estado delicado, indicación explícita de camilla).
- Usa Silla solo con evidencia visible de que puede ir sentado o indicación explícita de silla.
- Edad o sexo por sí solos NO justifican Silla ni Camilla.
- Si no hay evidencia suficiente usa Por definir.
- transportReason debe citar brevemente la evidencia visible usada.

OXÍGENO:
- oxygenProbable=true solo con evidencia visible de oxígeno indicado/usado, hipoxemia o soporte respiratorio. Si no, false.`;

const clean=v=>String(v??'').trim();
const plain=v=>clean(v).toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,' ').trim();
const uid=()=>crypto.randomUUID?.()||`${Date.now()}-${Math.random().toString(16).slice(2)}`;
const now=()=>new Date().toISOString();
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function read(key,fallback){try{const raw=localStorage.getItem(key);return raw?JSON.parse(raw):fallback}catch{return fallback}}
function write(key,val){localStorage.setItem(key,JSON.stringify(val))}
function currentRows(){const v=read(STORAGE_KEY,[]);return Array.isArray(v)?v:[]}
function shift(){let s=read(SHIFT_KEY,null);if(!s?.id){s={id:uid(),startedAt:now(),status:'ACTIVE'};write(SHIFT_KEY,s)}return s}
function normalizeBed(v){const raw=clean(v);if(!raw||/sala\s+de\s+espera/i.test(raw))return'';let s=raw.replace(/^C\/\s*(?=CE\s*\d+)/i,'').trim().toUpperCase().replace(/\s+/g,'');let m=s.match(/^CE0*(\d+)$/);if(m)return`CE${Number(m[1])}`;m=s.match(/^UP0*(\d+)$/);if(m)return`UP${Number(m[1])}`;m=s.match(/^UI0*(\d+)$/);if(m)return`UI${Number(m[1])}`;m=s.match(/^UA0*(\d+)$/);if(m)return String(Number(m[1]));m=s.match(/^C(?:AMA)?#?0*(\d+)$/);if(m)return String(Number(m[1]));if(/^0*\d+$/.test(s))return String(Number(s));return raw}
function normSex(v){const t=plain(v);if(['mujer','femenino','femenina','f'].includes(t))return'Mujer';if(['hombre','masculino','masculina','m','h'].includes(t))return'Hombre';return'No visible'}
function categoryOf(p){const c=plain(p?.category),m=plain(p?.modality),t=plain(p?.target||p?.study);if(c==='piso')return'Piso';if(c==='tac'||m==='tac'||/\b(tac|tc|tomografia|angiotac)\b/.test(t))return'TAC';if(c==='usg'||c==='ultrasonido'||m==='ultrasonido'||/\b(usg|ultrasonido|ecografia)\b/.test(t))return'USG';if(c==='interconsulta')return'Interconsulta';if(c.includes('apoyo'))return'Apoyo para movimiento';return'RX'}
function ageFromBirth(v){const s=clean(v);if(!/^\d{4}-\d{2}-\d{2}$/.test(s))return null;const d=new Date(`${s}T12:00:00`);if(Number.isNaN(d.getTime()))return null;const td=new Date();let a=td.getFullYear()-d.getFullYear();const md=td.getMonth()-d.getMonth();if(md<0||(md===0&&td.getDate()<d.getDate()))a--;return a>=0&&a<=130?a:null}
function inferredDestination(row){const bed=Number(String(row.destination||'').match(/\d+/)?.[0]);if(Number.isFinite(bed)&&bed>0){if(bed<=44)return{destinationFloor:'Primero',destinationBlock:'B'};if(bed<=88)return{destinationFloor:'Segundo',destinationBlock:'B'};if(bed<=132)return{destinationFloor:'Tercero',destinationBlock:'B'};if(bed<=165)return{destinationFloor:'Segundo',destinationBlock:'A'};if(bed<=198)return{destinationFloor:'Tercero',destinationBlock:'A'};if(bed<=231)return{destinationFloor:'Quinto',destinationBlock:'A'}}const s=plain(row.destinationService);if(/nefrolog/.test(s))return{destinationFloor:'Primero',destinationBlock:'B'};if(/cirugia|trauma/.test(s))return{destinationFloor:'Segundo',destinationBlock:'B'};if(/medicina interna|\bmi\b/.test(s))return{destinationFloor:'Tercero',destinationBlock:'B'};if(/obstetric/.test(s))return{destinationFloor:'Segundo',destinationBlock:'A'};if(/pediatr/.test(s))return{destinationFloor:'Tercero',destinationBlock:'A'};if(/ginec/.test(s))return{destinationFloor:'Quinto',destinationBlock:'A'};return{}}
function transportOf(p,target){const t=plain(p?.transport),e=plain(`${p?.transportReason||''} ${p?.diagnosis||''} ${p?.transferNotes||''} ${p?.recognizedText||''}`);if(/portatil/.test(plain(target))||t.includes('no traslad'))return['No trasladar',clean(p?.transportReason)||'Estudio portátil visible'];if(t.includes('camilla'))return['Camilla',clean(p?.transportReason)||'Indicación visible de camilla'];if(t.includes('silla'))return['Silla',clean(p?.transportReason)||'Indicación visible de silla'];if(/\bcamilla\b/.test(e))return['Camilla','Camilla indicada en la boleta'];if(/\bsilla\b/.test(e))return['Silla','Silla indicada en la boleta'];if(/inmovil|no deambula|encamado|fractura|trauma grave|deficit neurolog|hemiplej|estado delicado/.test(e))return['Camilla',clean(p?.transportReason)||'Hallazgo visible que limita el traslado sentado'];return['Por definir',clean(p?.transportReason)]}
function normalizeRow(p,fp,shiftId){const category=categoryOf(p),bed=normalizeBed(p?.handwrittenBed)||normalizeBed(p?.formBed)||normalizeBed(p?.bed);const rawTarget=clean(p?.target||p?.study||p?.destination);const target=category==='Piso'?clean(p?.destination||rawTarget):normalizeStudyDisplay(rawTarget);const [transport,transportReason]=transportOf(p,target);const row={...p,id:uid(),shiftId,category,bed,name:clean(p?.name),birthDate:clean(p?.birthDate),age:normalizeAge(p?.age)??ageFromBirth(p?.birthDate),sex:normSex(p?.sex),target,destination:category==='Piso'?clean(p?.destination||target):clean(p?.destination),destinationFloor:clean(p?.destinationFloor),destinationBlock:clean(p?.destinationBlock),destinationService:clean(p?.destinationService||((category==='Piso'&&!p?.originService)?p?.service:'')),modality:category==='TAC'?'TAC':category==='USG'?'Ultrasonido':category==='RX'?'Rayos X':clean(p?.modality)||'Otro',diagnosis:clean(p?.diagnosis),diagnosisMeaning:clean(p?.diagnosisMeaning),requestingDoctor:clean(p?.requestingDoctor||p?.doctor),folio:clean(p?.folio),service:category==='Piso'?clean(p?.originService):clean(p?.service),originService:clean(p?.originService),requestDate:clean(p?.requestDate),requestTime:clean(p?.requestTime),transferNotes:clean(p?.transferNotes),extraData:p?.extraData&&typeof p.extraData==='object'?p.extraData:{},recognizedText:clean(p?.recognizedText),transport,transportReason,oxygenProbable:Boolean(p?.oxygenProbable),oxygenReason:p?.oxygenProbable?clean(p?.oxygenReason):'',status:'Pendiente',imageFingerprint:fp,boletaImageFingerprint:fp,confidence:p?.confidence&&typeof p.confidence==='object'?p.confidence:{},needsReview:Object.values(p?.confidence||{}).some(v=>plain(v)==='low'),reviewFields:Object.entries(p?.confidence||{}).filter(([,v])=>plain(v)==='low').map(([k])=>k),manualOverrides:{},createdAt:now(),updatedAt:now()};if(category==='Piso'){const mapped=inferredDestination(row);if(!row.destinationFloor)row.destinationFloor=mapped.destinationFloor||'';if(!row.destinationBlock)row.destinationBlock=mapped.destinationBlock||''}return row}
function sameName(a,b){const A=plain(a),B=plain(b);return!!A&&!!B&&(A===B||(A.length>=6&&B.length>=6&&(A.includes(B)||B.includes(A))))}
function merge(existing,incoming){const o=existing.manualOverrides||{};const pick=f=>o[f]?existing[f]:(incoming[f]!==''&&incoming[f]!==null&&incoming[f]!==undefined?incoming[f]:existing[f]);return{...existing,...incoming,id:existing.id,createdAt:existing.createdAt,manualOverrides:o,bed:pick('bed'),name:pick('name'),age:pick('age'),sex:pick('sex'),target:pick('target'),transport:pick('transport'),transportReason:pick('transportReason'),destination:incoming.destination||existing.destination,destinationFloor:incoming.destinationFloor||existing.destinationFloor,destinationBlock:incoming.destinationBlock||existing.destinationBlock,destinationService:incoming.destinationService||existing.destinationService,requestingDoctor:incoming.requestingDoctor||existing.requestingDoctor,folio:incoming.folio||existing.folio,service:incoming.service||existing.service,originService:incoming.originService||existing.originService,requestDate:incoming.requestDate||existing.requestDate,requestTime:incoming.requestTime||existing.requestTime,transferNotes:incoming.transferNotes||existing.transferNotes,extraData:{...(existing.extraData||{}),...(incoming.extraData||{})},recognizedText:incoming.recognizedText||existing.recognizedText,boletaImageFingerprint:incoming.boletaImageFingerprint||existing.boletaImageFingerprint,imageFingerprint:incoming.imageFingerprint||existing.imageFingerprint,updatedAt:now()}}
function isHeaderOnly(p){const t=plain(p?.recognizedText);return /^piso(?: total)? \d+$/.test(t)||t==='piso'||(/^\d+$/.test(t)&&plain(p?.category)==='piso')}
function commitPatients(patients,fp,shiftId){const list=currentRows();let added=0,updated=0,partials=0,skipped=0;for(const p of Array.isArray(patients)?patients:[]){const incoming=normalizeRow(p,fp,shiftId);if(incoming.category==='Piso'&&!incoming.bed){if(isHeaderOnly(p))continue;if(incoming.name||incoming.destinationService||incoming.recognizedText)partials++;continue}if(incoming.category!=='Piso'&&!incoming.bed&&!incoming.name&&!incoming.target)continue;const exact=list.find(r=>r.shiftId===shiftId&&r.imageFingerprint===fp&&categoryOf(r)===incoming.category&&((incoming.bed&&canonicalOrigin(r.bed)===canonicalOrigin(incoming.bed))||(incoming.name&&sameName(r.name,incoming.name))));if(exact){if(plain(exact.status)!=='realizado'){const i=list.findIndex(r=>r.id===exact.id);list[i]=merge(exact,incoming);updated++}else skipped++;continue}if(incoming.category==='Piso'){const occupied=list.find(r=>r.shiftId===shiftId&&plain(r.status)!=='realizado'&&categoryOf(r)==='Piso'&&canonicalOrigin(r.bed)===canonicalOrigin(incoming.bed));if(occupied){if(sameName(occupied.name,incoming.name)||(!occupied.name&&!incoming.name)){const i=list.findIndex(r=>r.id===occupied.id);list[i]=merge(occupied,incoming);updated++}else skipped++;continue}}list.unshift(incoming);added++}write(STORAGE_KEY,list);syncRowsFromStorageAndRender();window.dispatchEvent(new CustomEvent('pendientes:v75-updated'));return{added,updated,partials,skipped}}
async function fingerprint(file){const buf=await file.arrayBuffer(),hash=await crypto.subtle.digest('SHA-256',buf);return[...new Uint8Array(hash)].map(b=>b.toString(16).padStart(2,'0')).join('')}
function db(){return new Promise((resolve,reject)=>{const req=indexedDB.open(DB_NAME,1);req.onupgradeneeded=()=>req.result.createObjectStore(STORE,{keyPath:'fp'});req.onsuccess=()=>resolve(req.result);req.onerror=()=>reject(req.error)})}
async function saveImage(fp,file){try{const d=await db();await new Promise((resolve,reject)=>{const tx=d.transaction(STORE,'readwrite');tx.objectStore(STORE).put({fp,blob:file,name:file.name||'boleta.jpg',type:file.type||'image/jpeg',createdAt:now()});tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error)});d.close()}catch{}}
async function loadImage(fp){if(!fp)return null;try{const d=await db();const value=await new Promise((resolve,reject)=>{const tx=d.transaction(STORE,'readonly'),r=tx.objectStore(STORE).get(fp);r.onsuccess=()=>resolve(r.result||null);r.onerror=()=>reject(r.error)});d.close();return value?.blob||null}catch{return null}}
function parseVision(v){if(v&&typeof v==='object')return v;const s=clean(v),f=s.match(/```(?:json)?\s*([\s\S]*?)```/i)?.[1]?.trim()||s;try{return JSON.parse(f)}catch{const a=f.indexOf('{'),b=f.lastIndexOf('}');if(a>=0&&b>a)return JSON.parse(f.slice(a,b+1));throw new Error('Respuesta de visión inválida')}}
function toast(msg,tone=''){document.getElementById('v75Toast')?.remove();const el=document.createElement('div');el.id='v75Toast';el.className=`v75-toast ${tone}`;el.textContent=msg;document.body.appendChild(el);setTimeout(()=>el.remove(),2600)}
async function analyzeFile(file,index,total){if(!(file instanceof File)||!file.type.startsWith('image/'))throw new Error('Selecciona una imagen válida.');if(file.size>8*1024*1024)throw new Error(`${file.name||'La foto'} pesa más de 8 MB.`);setProgress(`Analizando foto ${index+1} de ${total}…`);const fp=await fingerprint(file);await saveImage(fp,file);const form=new FormData();form.append('image',file);form.append('prompt',VISION_PROMPT);const res=await fetch('/api/turno-rx/vision',{method:'POST',headers:{'X-Turno-RX':'1'},body:form,credentials:'same-origin'});const data=await res.json().catch(()=>({}));if(!res.ok)throw new Error(data.error||`Error ${res.status}`);const parsed=parseVision(data.text||data.answer||data.output_text||data),patients=Array.isArray(parsed?.patients)?parsed.patients:[parsed];return commitPatients(patients,fp,shift().id)}
let processing=false;
async function processFiles(files){if(processing)return;const arr=[...files];if(!arr.length)return;processing=true;let added=0,updated=0,errors=0,partials=0;for(let i=0;i<arr.length;i++){try{const r=await analyzeFile(arr[i],i,arr.length);added+=r.added;updated+=r.updated;partials+=r.partials}catch(e){errors++;toast(e?.message||'No se pudo analizar una foto','warning')}}processing=false;setProgress('');toast(`${added} agregados${updated?` · ${updated} actualizados`:''}${partials?` · ${partials} parciales`:''}${errors?` · ${errors} error${errors===1?'':'es'}`:''}`,errors?'warning':'success')}
function setProgress(text){const el=document.getElementById('captureStatus');if(!el)return;el.hidden=!text;el.textContent=text;el.dataset.state=text?'busy':''}
function detailFields(r){const fields=[['Cama / área',r.bed],['Nombre',r.name],['Edad',r.age!=null?`${r.age} años`:''],['Sexo',r.sex&&r.sex!=='No visible'?r.sex:''],['Categoría',r.category],['Estudio / destino',r.target||r.destination],['Servicio destino',r.destinationService],['Piso destino',[r.destinationFloor,r.destinationBlock].filter(Boolean).join(' ')],['Médico',r.requestingDoctor],['Folio',r.folio],['Servicio / origen',r.originService||r.service],['Fecha solicitud',r.requestDate],['Hora solicitud',r.requestTime],['Diagnóstico',r.diagnosis],['Notas',r.transferNotes],['Traslado',r.transport],['Motivo traslado',r.transportReason],['O₂ probable',r.oxygenProbable?'Sí':''],['Motivo O₂',r.oxygenReason]];for(const [k,v] of Object.entries(r.extraData||{}))if(v!==''&&v!=null)fields.push([k,v]);return fields.filter(([,v])=>v!==''&&v!=null)}
let bypassDetail=false;
async function openDetail(id){const r=currentRows().find(x=>String(x.id)===String(id));if(!r)return;document.getElementById('v75Detail')?.remove();const back=document.createElement('div');back.id='v75Detail';back.className='v75-backdrop';back.innerHTML=`<section class="v75-sheet"><div class="v75-handle"></div><header><div><small>BOLETA / PACIENTE</small><h2>${esc(r.name||r.bed||'Detalle')}</h2><span>${esc(r.category||'')}</span></div><button type="button" data-v75-close>×</button></header><div class="v75-photo" data-v75-photo><div class="v75-photo-empty">Cargando foto de boleta…</div></div><div class="v75-fields">${detailFields(r).map(([k,v])=>`<div><small>${esc(k)}</small><strong>${esc(typeof v==='object'?JSON.stringify(v):v)}</strong></div>`).join('')}</div>${r.recognizedText?`<details><summary>Texto reconocido</summary><p>${esc(r.recognizedText)}</p></details>`:''}<footer><button type="button" data-v75-edit="${esc(r.id)}">Editar</button><button type="button" data-v75-close>Cerrar</button></footer></section>`;back.addEventListener('click',e=>{if(e.target===back||e.target.closest('[data-v75-close]'))back.remove();const edit=e.target.closest('[data-v75-edit]');if(edit){back.remove();const row=document.querySelector(`.patient-row[data-id="${CSS.escape(String(edit.dataset.v75Edit))}"]`);if(row){bypassDetail=true;row.click();queueMicrotask(()=>bypassDetail=false)}}});document.body.appendChild(back);const blob=await loadImage(r.boletaImageFingerprint||r.imageFingerprint);const slot=back.querySelector('[data-v75-photo]');if(!slot)return;if(blob){const url=URL.createObjectURL(blob),img=new Image();img.alt='Foto original de la boleta';img.onload=()=>URL.revokeObjectURL(url);img.src=url;slot.innerHTML='';slot.appendChild(img)}else slot.innerHTML='<div class="v75-photo-empty">Este registro no tiene foto guardada.</div>'}

document.addEventListener('change',e=>{if(e.target.id!=='galleryInput'&&e.target.id!=='cameraInput')return;e.preventDefault();e.stopImmediatePropagation();const files=e.target.files;e.target.value='';processFiles(files)},true);
document.addEventListener('click',e=>{const row=e.target.closest('.patient-row[data-id]');if(row&&!bypassDetail&&!e.target.closest('button,input,select,textarea,label,[data-quick-transport],[data-stability-transport]')){e.preventDefault();e.stopImmediatePropagation();openDetail(row.dataset.id)}},true);
window.addEventListener('pendientes:v75-updated',()=>queueMicrotask(()=>document.documentElement.dataset.pendientesBuild=BUILD));
document.documentElement.dataset.pendientesBuild=BUILD;
