import {describe,expect,it} from 'vitest';
import {aggregateDeliberationStatus} from './deliberation-status';

describe('aggregateDeliberationStatus',()=>{
 it('aggregates modes, passes, cost and health without content',()=>{const now=Date.parse('2026-07-21T16:00:00Z');const result=aggregateDeliberationStatus([{service:'contextual-chat',model:'reasoning',estimated_cost_usd:.12,created_at:'2026-07-21 15:00:00',metadata_json:JSON.stringify({cognitiveMode:'ensemble',deliberationPasses:3,deliberationReason:'tarea profunda'})},{service:'contextual-chat',model:'reasoning',estimated_cost_usd:.08,created_at:'2026-07-21 14:00:00',metadata_json:JSON.stringify({cognitiveMode:'ensemble-degraded',deliberationPasses:2,deliberationReason:'juez no disponible'})},{service:'contextual-chat',model:'fast',estimated_cost_usd:.01,created_at:'2026-07-10 10:00:00',metadata_json:JSON.stringify({cognitiveMode:'single',deliberationPasses:1})},{service:'vision',model:'vision',estimated_cost_usd:2,created_at:'2026-07-21 15:30:00',metadata_json:'{}'}],now);expect(result.calls30d).toBe(3);expect(result.calls24h).toBe(2);expect(result.ensemble30d).toBe(2);expect(result.degraded30d).toBe(1);expect(result.averagePasses).toBe(2);expect(result.estimatedCostUsd30d).toBe(.21);expect(result.estimatedCostUsd24h).toBe(.2);expect(result.health).toBe('watch');expect(result.contentStored).toBe(false);expect(JSON.stringify(result)).not.toContain('prompt');});
 it('ignores malformed metadata and reports idle state',()=>{const result=aggregateDeliberationStatus([{metadata_json:'not-json',estimated_cost_usd:5,created_at:'2026-07-21 15:00:00'}],Date.parse('2026-07-21T16:00:00Z'));expect(result.calls30d).toBe(0);expect(result.estimatedCostUsd30d).toBe(0);expect(result.health).toBe('idle');expect(result.last).toBeNull();});
});
