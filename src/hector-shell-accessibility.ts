function labelButton(button:HTMLButtonElement|null,label:string){if(button&&!button.getAttribute('aria-label'))button.setAttribute('aria-label',label)}
function syncNavigation(){for(const button of document.querySelectorAll<HTMLButtonElement>('.cxSidebar nav button')){if(button.classList.contains('active'))button.setAttribute('aria-current','page');else button.removeAttribute('aria-current')}}
function syncPanels(){const sidebar=document.querySelector<HTMLElement>('.cxSidebar'),history=document.querySelector<HTMLElement>('.cxHistory');if(sidebar){sidebar.setAttribute('aria-label','Navegación principal');sidebar.setAttribute('aria-hidden',String(!sidebar.classList.contains('open')&&matchMedia('(max-width: 760px)').matches));labelButton(sidebar.querySelector('.cxClose'),'Cerrar menú');}if(history){history.setAttribute('aria-label','Conversaciones');history.setAttribute('aria-hidden',String(!history.classList.contains('open')&&matchMedia('(max-width: 980px)').matches));labelButton(history.querySelector('.cxHistoryHead button'),'Cerrar conversaciones');}}
function syncComposer(){const textarea=document.querySelector<HTMLTextAreaElement>('.cxComposer textarea');if(textarea&&!textarea.getAttribute('aria-label'))textarea.setAttribute('aria-label','Mensaje para Héctor OS');const thinking=document.querySelector<HTMLElement>('.cxThinking');if(thinking){thinking.setAttribute('role','status');thinking.setAttribute('aria-live','polite');thinking.setAttribute('aria-label','Héctor OS está pensando');}}
export function installHectorShellAccessibility(){let scheduled=false;const sync=()=>{if(scheduled)return;scheduled=true;requestAnimationFrame(()=>{scheduled=false;syncNavigation();syncPanels();syncComposer();});};const observer=new MutationObserver(sync);observer.observe(document.body,{childList:true,subtree:true,attributes:true,attributeFilter:['class']});window.addEventListener('resize',sync);sync();return()=>{observer.disconnect();window.removeEventListener('resize',sync);};}
